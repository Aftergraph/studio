import { validateIntegrationManifest } from '../federation/integration-manifest.mjs';
import { createEnvelope } from '../federation/object-envelope.mjs';
function items(snapshot,key,type,revision){return (snapshot[key]??[]).map(x=>createEnvelope({sourceIntegration:'avc',type,canonicalId:String(x.id),canonicalOwner:'avc',tenantId:x.tenantId??null,status:x.status??'unknown',freshness:snapshot.freshness??'stale',payload:x,sourceRevision:revision,authority:[{owner:'avc',operations:Array.isArray(x.operations)?x.operations:[]}],evidence:Array.isArray(x.evidence)?x.evidence:[]}))}
export function createAvcManifest({repositoryRevision}={}){if(!repositoryRevision)throw new TypeError('repositoryRevision required');return validateIntegrationManifest({schema:'aftergraph.integration/v1',id:'avc',repository:'Aftergraph/autonomous-venture-company',integrationVersion:'1.0.0',repositoryRevision,roles:['venture-runtime','company-kernel','agent-orchestration'],objects:['agent','mission','product_cell','incident','evidence'],relations:['assigned_to','belongs_to','produces'],capabilities:['mission.propose','mission.control','agent.assign'],surfaces:['agents','ventures','control'],events:[],reads:['agents','missions','product-cells','incidents','evidence'],writes:[],authority:[{owner:'avc',operations:['mission-authorize','agent-assign','venture-control']}],evidence:[{owner:'avc',class:'venture-runtime'}],health:{kind:'external-contract'},degradedBehavior:{reads:'stale',writes:'block'},compatibility:{mode:'exact-or-declared',supported:['1.x']}})}
export function projectAvcSnapshot(snapshot={}){const revision=snapshot.revision;if(!revision)throw new TypeError('revision required');const objects=[...items(snapshot,'agents','agent',revision),...items(snapshot,'missions','mission',revision),...items(snapshot,'productCells','product_cell',revision),...items(snapshot,'incidents','incident',revision)];const evidence=(snapshot.evidence??[]).map(e=>Object.freeze({id:`avc:${e.id}`,owner:'avc',class:'venture-runtime',method:e.method??'unknown',freshness:snapshot.freshness??'stale',status:e.status??'unknown',sourceRevision:revision,payload:Object.freeze(structuredClone(e))}));return Object.freeze({objects:Object.freeze(objects),evidence:Object.freeze(evidence),relations:Object.freeze(structuredClone(snapshot.relations??[]))})}
