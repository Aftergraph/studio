export function resolveAuthority(object,operation){const ref=object?.authority?.find(a=>Array.isArray(a.operations)&&a.operations.includes(operation));if(!ref?.owner)throw new Error(`authority unavailable for ${operation}`);return Object.freeze(structuredClone(ref))}
export async function executeConsequentialWrite({object,operation,input,registry,adapters,onUiState=()=>{}}){const authority=resolveAuthority(object,operation);if(!registry?.canWrite?.(authority.owner))throw new Error(`authority owner ${authority.owner} not current`);const adapter=adapters?.[authority.owner];if(!adapter?.write)throw new Error(`authority adapter ${authority.owner} unavailable`);onUiState('submitted');const result=await adapter.write(operation,input);onUiState('resyncing');return result}
