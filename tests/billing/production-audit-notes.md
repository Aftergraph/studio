Production invariants under audit:
- installable shell must be complete offline for read-only use
- legacy navigation must comply with Studio CSP
- authenticated Billing sessions must not depend on demo-user fallback
- offline data must never leak across authenticated identities
